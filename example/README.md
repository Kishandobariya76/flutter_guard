# FlutterGuard example

Runnable demo of every FlutterGuard module. It uses a local `DemoTransport`
and does not need a production backend.

```bash
flutter pub get
flutter run
```

The home dashboard opens a dedicated screen for each feature, plus a
configuration playground and the in-app inspector.
